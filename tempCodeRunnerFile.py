    # for piece, (col, row) in piece_positions.items():
    #     if piece.startswith("b"):
    #         if piece == "b_pawn":
    #             for i in range(8):
    #                 screen.blit(pieces_black[piece], (i * SQUARE_SIZE, row * SQUARE_SIZE))
    #         else:
    #             screen.blit(pieces_black[piece], (col * SQUARE_SIZE, row * SQUARE_SIZE))
    #     else:
    #         if piece == "w_pawn":
    #             for i in range(8):
    #                 screen.blit(pieces_white[piece], (i * SQUARE_SIZE, row * SQUARE_SIZE))
    #         else:
    #             screen.blit(pieces_white[piece], (col * SQUARE_SIZE, row * SQUARE_SIZE))